{
  "name": "@opentechevents/dashboard-checks",
  "version": "0.1.0",
  "description": "Client-side setup checks + template-update banner for OTE organizer dashboards (served from tools.opentechevents.org)",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
